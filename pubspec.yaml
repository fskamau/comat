name: comat
description: "comrade market"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8

  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  google_sign_in: ^6.2.1

  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  file_picker: ^8.0.0+1
  path: ^1.9.0

  flutter_dotenv: ^5.1.0


  # AI & Utils
  google_generative_ai: ^0.4.0
  url_launcher: ^6.3.0
  camera: ^0.10.5+9
  permission_handler: ^11.3.0
  cached_network_image: ^3.4.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true
  image_path: "assets/images/logos/comat_icon.png"

flutter:
  uses-material-design: true
  assets:
    - .env